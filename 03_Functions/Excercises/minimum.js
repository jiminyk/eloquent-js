

// write a function that returns the minimum between 2 arguments

function minimum(a, b){
    if (a < b) {
        return a;
    } else {
        return b;
    }
}

console.log(minimum(9, -12)); // -12